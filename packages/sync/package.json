{
  "name": "@k2b/sync",
  "version": "6.3.2",
  "description": "NATS-native distributed sync primitives for TypeScript and Bun \u2014 queue, topic, job, pump, scheduler, mutex, ephemeral, object store, retry",
  "module": "index.ts",
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./retry": "./retry.ts"
  },
  "scripts": {
    "test": "bun test --preload ./tests/preload.ts tests/*.test.ts",
    "test:serial": "bun test --preload ./tests/preload.ts tests/serial/fault.test.ts && bun test --preload ./tests/preload.ts tests/serial/probe.test.ts",
    "test:all": "bun run test && bun run test:serial",
    "typecheck": "bunx tsc --noEmit"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@nats-io/jetstream": "3.4.0",
    "@nats-io/kv": "3.4.0",
    "@nats-io/nats-core": "3.4.0",
    "@nats-io/obj": "3.4.0"
  },
  "devDependencies": {
    "@nats-io/transport-node": "3.4.0"
  }
}
